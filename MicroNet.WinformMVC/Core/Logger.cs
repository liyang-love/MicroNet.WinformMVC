using System;

namespace    MicroNet.WinformMVC.Core
{
    static class Logger
	{
	    internal static Action<string> Error = (message) =>
	    {
	    };

        internal static Action<string> Info = (message) =>
	    {
	    };
	}
}